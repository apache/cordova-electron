{
  "linux": [],
  "config": {
    "linux": {
      "maintainer": "${APP_AUTHOR}",
      "icon": "${APP_INSTALLER_ICON}",
      "target": [
        {
          "target": "tar.gz",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  }
}
